"use strict";
describe("array", () => {
    it("should same with js", () => {
        const names = ["Kahfi", "Smith"];
        const values = [1, 2, 3];
        console.info(names);
        console.info(values);
    });
});
