"use strict";
describe('tipe data', () => {
    it('should return "Hello, World!"', () => {
        const name = 'Kahfi Smith';
        const balance = 100000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
