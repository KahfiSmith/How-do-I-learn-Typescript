describe("array", () => {
  it("should same with js", () => {
    const names: string[] = ["Kahfi", "Smith"];
    const values: number[] = [1, 2, 3];

    console.info(names);
    console.info(values);
  });
});
