describe("tipe data", () => {
  it('should return "Done"', () => {
    const name: string = "Kahfi Smith";
    const balance: number = 100000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
